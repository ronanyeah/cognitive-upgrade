{
  "dependencies": {
    "elm": "^0.19.0-bugfix2"
  },
  "devDependencies": {
    "elm-live": "^3.0.1"
  },
  "scripts": {
    "watch": "elm-live --dir=public src/Main.elm -- --output=public/index.html",
    "build": "elm make src/Main.elm --optimize --output=public/index.html"
  }
}
